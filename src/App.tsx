import AppLayout from './components/AppLayout'
import { Button } from 'antd'

function App() {
    return (
        <AppLayout>
            <div>
                <h1>Vite + React</h1>
                <Button>count is 0</Button>
            </div>
        </AppLayout>
    )
}

export default App
